services:
  integration:
    image: mysql:8.0
    container_name: mysql-db
    restart: always
    environment:
      - MYSQL_DATABASE=${SQL_DATABASE}
      - MYSQL_USER=${SQL_USER}
      - MYSQL_PASSWORD=${SQL_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${SQL_ROOT_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - ./Config/Sql/mysql-init:/docker-entrypoint-initdb.d
    networks:
      - shared-test-network

volumes:
  mysql-data:

networks:
  shared-test-network:
    external: true
